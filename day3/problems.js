// solved problems from website: JSchallenger
// javascript fundamentals

// check if a numbera whole numba
function myFunc(a){
    return (a * 10) % 10 === 0 ? true : false;
};

function myFunc1(a){
    return a - Math.floor(a) === 0;
};

// multipltion, dividing, and comparison operators;
function myFunction(a, b){
    return a < b ? a / b : a * b;
};

// round a number to decimal places
function myFunction(a) {
    return Math.round(a * 100) / 100;
};

function myFunction1(a) {
    return Number(a.toFixed(2));
}

//split numbers into digits